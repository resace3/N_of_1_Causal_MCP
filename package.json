{
  "name": "patient-causal-mcp",
  "private": true,
  "description": "Cloudflare Builds entrypoint for the Python patient causal MCP server.",
  "scripts": {
    "deploy": "bash scripts/cf-deploy.sh",
    "dev": "uv run pywrangler dev",
    "test": "python -m pytest",
    "test:cf-imports": "python -m pytest tests/test_cloudflare_worker_imports.py"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
